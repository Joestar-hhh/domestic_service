<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cykj.domestic.mapper.CompanyMapper">

    <select id="queryRegion" resultType="com.cykj.domestic.entity.Company">
             SELECT B.region region,COUNT(B.region)regionId,MAX(A.join_time)joinTime
             FROM tb_company A LEFT JOIN tb_region B on A.region_id =B.id GROUP BY B.region;
    </select>

</mapper>